<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
  <Page.actionBar>
    <ActionBar title="Plugin Demos" icon="" class="action-bar"/>
  </Page.actionBar>
  <StackLayout class="p-20">
    <ScrollView class="h-full">
      <StackLayout>
        <Button text="add a package first" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="wcg-core" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="wcg-url" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="wcg-text-encoding" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="wcg-crypto" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
      </StackLayout>
    </ScrollView>
  </StackLayout>
</Page>